# Model Management Lambda Functions

This directory contains Lambda functions for AWS Bedrock model management:

- `finetune.ts` - Initiate model fine-tuning
- `getStatus.ts` - Check fine-tuning status
- `update.ts` - Trigger incremental fine-tuning

These will be implemented in Task 6.
