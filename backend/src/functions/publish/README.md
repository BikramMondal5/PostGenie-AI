# Publishing Lambda Functions

This directory contains Lambda functions for content publishing:

- `publishSingle.ts` - Publish to a single platform
- `publishMulti.ts` - Publish to multiple platforms
- `getHistory.ts` - Retrieve post history

These will be implemented in Task 11.
